# Copy this file to .env in the primary repository checkout.
# Dodai worktrees reuse that ignored local file automatically.
OPENAI_API_KEY=
# Optional when Codex CLI is not already authenticated.
CODEX_API_KEY=
